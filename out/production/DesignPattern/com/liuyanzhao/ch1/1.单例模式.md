## 单例模式

#### 一、特征
保证一个类仅有一个实例，并提供一个访问它的全局访问点

#### 二、优点
1. 在内存中只有一个实例，减少内存开支，尤其是针对需要频繁创建和销毁的对象
2. 提供唯一的访问点，方便优化


#### 三、缺点
1. 不好扩展，若要扩展必须修改原代码


#### 四、应用场景
1. 创建一个对象需要消耗的资源很多，如要访问IO或数据库
2. 要求生成唯一的序列号
